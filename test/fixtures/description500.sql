  INSERT INTO `description500` (
    desc500,
    fk_id,
    fk_table
  ) VALUES (
    'Check markings on cases, there was a problem with past orders. This is general information only, not to be processed by your system',
    1, 
    'orders'
  );
  
  INSERT INTO `description500` (
    desc500,
    fk_id,
    fk_table
  ) VALUES (
    'Check markings on cases, there was a problem with past orders. This is general information only, not to be processed by your system',
    2, 
    'orders'
  );
  
   INSERT INTO `description500` (
    desc500,
    fk_id,
    fk_table
  ) VALUES (
    'Check markings on cases, there was a problem with past orders. This is general information only, not to be processed by your system',
    1, 
    'order_line_items'
  );
  
  INSERT INTO `description500` (
    desc500,
    fk_id,
    fk_table
  ) VALUES (
    'Check markings on cases, there was a problem with past orders. This is general information only, not to be processed by your system',
    3, 
    'order_line_items'
  );
